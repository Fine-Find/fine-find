import { BasicProfileForm as BasicProfile } from './';

export default {
  title: 'Profile/Basic Profile Form',
  component: BasicProfile,
};

export const BasicProfileForm = (args) => <BasicProfile {...args} />;
